[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = [
  "probe-run",
  "--chip",
  "STM32F303CBTx",
  "--log-format",
  "{t} [ {L}] {f}:{l} {s}"
]

[build]
target = "thumbv6m-none-eabi"
rustflags = ["-Z", "macro-backtrace"]

[env]
DEFMT_LOG = "debug"
